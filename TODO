don't display db password in setup-aws-acct

reduce permissions for server role

test google analytics on http and https paths

have stack create automatically update auto scaling groups in all
deployment groups

remove test-key-pair (or work out a better way - maybe stack creator's key)

healthcheck uses /www/blank/index.html - change to index.php and check
database connection
